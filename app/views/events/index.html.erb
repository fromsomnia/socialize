<div class="container">
	
	<% for event in @events do %>
		<% creator = User.find_by_id(event.user_id) %>

			<div class="container event center-block">
				<h3>
					<a href="/events/index/<%= event.user_id %>"><%= event.title %></a>
				</h3>
				<img src="<%= creator.image_url %>" class="img-responsive img-rounded" alt="<%= creator.first_name %>">
				<h4>By <%= creator.first_name + " " + creator.last_name %></h4>

				<p><%= event.description %></p>

				<ul>
					<li>Place: <%= event.place %></li>
					<li>Date: <%= event.date %></li>
					<li>Time: <%= event.time %></li>
				</ul>
				<div>
					<h4>Also Going:</h4>
					<hr>
					<% for attendee in event.users do %>
						<a href="/users/index/<%= attendee.id %>">
							<div class="inline-icon-container">
								<div class="inline-icon">
										<img src ="<%= attendee.image_url %>" class="img-responsive img-rounded icon-image" alt="<%= attendee.first_name %>">
								</div>
								<div class="icon-title">
									<p><%= attendee.first_name %></p>
								</div>
							</div>
						</a>
					<% end %>
					<hr>
				</div>
			</div>
		<hr>

	<% end %>

	<!form id="addFriendForm" role="form" method="get" action="/add">
		<!h3Add a friend</h3>
	    <!div class="form-group">
	      <!label for="name"Name:</label>
	      <!input type="text" class="form-control" id="name" placeholder="Name" name="name">
	    <!/div>
	    <!div class="form-group">
	      <!label for="description"Description:</label>
	      <!nput type="text" class="form-control" id="description" placeholder="Description" name="description">
	    <!/div>
	    <!input type="submit" id="submitBtn" class="btn btn-default" value="Add the friend"</input>
	<!/form>

</div>